<div class= "row">
  <div class = "col-md-8 col-md-offset-2">
    <h1>Ifarms Community!</h1>
    <p>Welcome to the ifarms community. User must fill out a profile in order to be visible in the community!</p>
    <ul class= "list-unstyled">
    <% @user.each do |user| %>
    
      <% if user.profile %>
    <li>
      <%= user.profile %><br/>
    <% end %>
      <li>
    <% end %>
      </ul>
  </div>
</div>